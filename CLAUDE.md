# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository Status

This repository is new and currently empty. It is named `clauderutinas`, suggesting its purpose is to house Claude Code routines, automations, or scheduled tasks.

## Intended Purpose

As the repository grows, update this file to document:

- **Build / run commands** — how to install dependencies, start the app, run tests, and lint.
- **Architecture** — the high-level structure once components are added (e.g. trigger definitions, skill files, scheduled-task scripts).
- **Key conventions** — naming patterns, directory layout, and any project-specific tooling decisions.

## Development Branch

Active development happens on feature branches. The default branch to target for new work is whatever is set as the repository default (check `git remote show origin` if unsure).

## Getting Started (once files exist)

```bash
git clone https://github.com/ventaaira-code/clauderutinas.git
cd clauderutinas
```

Update this section with actual setup steps once the stack is decided.
